"""On-chain screener for freshly launched DEX pairs.

The package deliberately produces a *rejection* verdict, never a buy signal:
every rule can disqualify a token, none can endorse one.
"""

__version__ = "0.1.0"
